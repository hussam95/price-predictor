# OneMap Search requires a token as of 1 October 2025.
# Preferred when you already generated a current token (tokens expire after 3 days):
ONEMAP_TOKEN=

# Alternative: the geocoding command can request a token without logging credentials.
ONEMAP_EMAIL=
ONEMAP_PASSWORD=
